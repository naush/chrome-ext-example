import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#FF6347',
    },
    secondary: {
      main: '#D7B98C',
    },
    background: {
      paper: '#FFE9E5',
    },
  },
});

export default theme;

import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#FF6347',
    },
    secondary: {
      main: '#9D39B5',
    },
    background: {
      paper: '#FFE9E5',
    },
  },
});

export default theme;
